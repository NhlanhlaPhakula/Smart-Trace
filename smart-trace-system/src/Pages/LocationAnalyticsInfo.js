import React from 'react';
import firebase from '../Components/Firebase';

const SowetoLists = ({names}) => {
    return(
        <div>
            <h5>{names.category}</h5>
        </div>
    );
};
export default SowetoLists;

const SandtonLists = ({names}) => {
    return(
        <div>
            <h5>{names.category}</h5>
        </div>
    );
};

const JohannesburgLists = ({names}) => {
    return(
        <div>
            <h5>{names.category}</h5>
        </div>
    );
};

const MidrandLists = ({names}) => {
    return(
        <div>
            <h5>{names.category}</h5>
        </div>
    );
};

const AlexandraLists = ({names}) => {
    return(
        <div>
            <h5>{names.category}</h5>
        </div>
    );
};

const RoodepoortLists = ({names}) => {
    return(
        <div>
            <h5>{names.category}</h5>
        </div>
    );
};

export {SandtonLists,JohannesburgLists,MidrandLists,AlexandraLists,RoodepoortLists}

